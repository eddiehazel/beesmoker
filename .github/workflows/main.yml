name: run smoke tests.....

on:
  push:
  schedule:
    - cron: '0 * * * *'

jobs:
  smoke_test:
    name: run smoke tests
    runs-on: ubuntu-latest
    steps:
      - name: run tests
        env: 
          FAUCET_TOKEN: ${{ secrets.FAUCET_TOKEN }}
          TEST: ${{ secrets.TEST }}  
        run: "echo ${{ secrets.TEST }}"
