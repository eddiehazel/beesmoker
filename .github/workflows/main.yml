name: run smoke tests.....

on:
  push:
  schedule:
    - cron: '0 * * * *'

jobs:
  smoke_test:
    name: run smoke tests
    runs-on: ubuntu-latest
    steps:
      - name: run tests
          run: "echo ${{ secrets.FAUCET_TOKEN }}"
